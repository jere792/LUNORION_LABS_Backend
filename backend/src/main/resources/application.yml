spring:
  application:
    name: lunorion-labs
  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/lunorion_db
    username: postgres
    password: dominid
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        default_schema: public

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:lunorion-labs-secret-key-change-in-production}
  expiration: 86400000
